

# Buttons required

    - manual run
    will stop after each call breakpoint

    I need the button handler to start target, start rad
    
    - auto run
    will not stop continue to run until process exits and notes all the calls


    - step
    in case of manual run steps into the next call breakpoint
    just to send the run command
